# Vercel Python function deps (backend/api). The heavy ML deps live in the
# root requirements.txt — this mirrors the backend's needs for the function.
mangum
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.2.3
pytest==9.1.1
httpx==0.28.1